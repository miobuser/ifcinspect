{
  "name": "ifcinspect-web",
  "version": "0.4.41",
  "private": true,
  "type": "module",
  "description": "Browser-native IfcInspect — runs entirely in the client via Pyodide. IFC never leaves your browser.",
  "scripts": {
    "dev": "vite",
    "build:wheel": "node scripts/build-wheel.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pdfmake": "^0.2.23",
    "three": "^0.184.0",
    "web-ifc": "^0.0.75"
  },
  "devDependencies": {
    "@types/pdfmake": "^0.2.13",
    "@types/three": "^0.184.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
